# SCK Security Framework Dataset Ingestion Environment Variables (FREE VERSION)
# Copy this file to .env and fill in your actual values

# 🆓 FREE EMBEDDINGS (No API costs!)
# Option 1: Local SentenceTransformers (100% FREE)
# No environment variables needed - runs locally

# Option 2: HuggingFace Inference API (FREE tier - 30K requests/month)
HUGGINGFACE_TOKEN=your-huggingface-token-here  # Optional, increases rate limits

# ☁️ FREE VECTOR DATABASE
# Option 1: Supabase Vector (FREE tier - 500MB storage, 2GB bandwidth)
SUPABASE_URL=your-supabase-project-url
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_TABLE=knowledge_chunks

# Option 2: Local PostgreSQL with pgvector (100% FREE)
# DATABASE_URL=postgresql://username:password@localhost:5432/sck_database

# 🔧 OPTIONAL CONFIGURATION
# Embedding model (local SentenceTransformers)
EMBEDDING_MODEL=all-mpnet-base-v2  # 768 dimensions, high quality

# Table name for Supabase Vector
SUPABASE_TABLE=knowledge_chunks

# 🎯 RECOMMENDED FREE SETUP:
# 1. Use local SentenceTransformers for embeddings (FREE)
# 2. Use Supabase Vector for storage (FREE tier)
# 3. Total cost: $0.00
