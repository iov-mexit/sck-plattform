{
  "buildCommand": "npm install --registry=https://registry.npmjs.com/ --fetch-retries=20 --fetch-retry-mintimeout=10000 --fetch-retry-maxtimeout=120000 && npm run build",
  "installCommand": "npm install --registry=https://registry.npmjs.com/ --fetch-retries=20 --fetch-retry-mintimeout=10000 --fetch-retry-maxtimeout=120000",
  "framework": "nextjs",
  "functions": {
    "app/api/v1/rag/search/route.ts": {
      "maxDuration": 30
    }
  }
}
