'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@/components/common/card';

export function SignalStream() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Signal Stream</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Coming soon: A real-time feed of signals.</p>
      </CardContent>
    </Card>
  );
}
