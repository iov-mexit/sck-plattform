name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      NODE_ENV: production
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
    steps:
      # 1️⃣ Checkout repo
      - uses: actions/checkout@v3

      # 2️⃣ Setup Node.js
      - uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: 'yarn'

      # 3️⃣ Install dependencies at root (workspace-aware)
      - name: 📦 Install Yarn dependencies
        run: yarn install --immutable --immutable-cache --check-cache

      # 4️⃣ Ensure @prisma/client matches schema
      - name: 🔧 Prisma Generate
        run: yarn workspace @sck/web prisma generate

      # 5️⃣ Install Sharp for Linux platform
      - name: 🖼️ Install Sharp (Linux)
        run: |
          cd apps/web
          npm install --platform=linux --arch=x64 sharp

      # 6️⃣ Build Next.js App
      - name: 🔨 Build Next.js
        run: yarn workspace @sck/web build

      # 7️⃣ Deploy to Vercel
      - name: 📦 Deploy to Vercel
        run: |
          echo "Deploying @sck/web to Vercel..."
          yarn workspace @sck/web vercel --prod --yes --token $VERCEL_TOKEN
